import 'package:flutter/material.dart';

const textColor = Color(0xFF424242); // Dark grey for general text
const shadoColor = Color(0xFFC4ACA1);
const primaryColor =
    Color(0xFF5A8D7E); // A muted olive green, inspired by the image
const secondaryColor = Color(0xFFA8B7A8); // A lighter, complementary green
const backgroundColor =
    Color(0xFFE0E0DE); // A soft, light beige-grey for scaffold background
const cardColor = Colors.white; // Pure white for cards
